import{CheckCircle2,Circle,Clock3}from'lucide-react';import Badge from'../common/Badge';const steps=['SUBMITTED','AI_ANALYZED','PENDING_VALIDATION','VALIDATED','ROUTED','ACCEPTED','COLLABORATION_OPEN','TEAM_FORMED','RESEARCH','PROTOTYPE','FIELD_TESTING','VALIDATION','DEPLOYED','IMPACT_TRACKING','COMPLETED'];export default function StatusTimeline({current}:{current:string}){const idx=steps.indexOf(current);return <div className="space-y-0">{steps.map((s,i)=>{const done=idx>=0&&i<idx,now=s===current;return <div key={s} className="flex gap-4"><div className="flex flex-col items-center">{done?<CheckCircle2 size={19} className="text-jharkhand-600"/>:now?<Clock3 size={19} className="text-amber-600"/>:<Circle size={19} className="text-slate-300"/>}{i<steps.length-1&&<div className={`w-px flex-1 min-h-7 ${done?'bg-jharkhand-200':'bg-slate-200'}`}/>}</div><div className={`pb-4 ${now?'font-bold':''}`}><div className="text-sm">{s.replaceAll('_',' ')}</div>{now&&<Badge tone="amber">Current stage</Badge>}</div></div>})}</div>}
