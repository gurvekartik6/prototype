import{useEffect,useState}from'react';import{api}from'../services/api';import{GraduationCap,Search}from'lucide-react';import Badge from'../components/common/Badge';export default function Institutions(){const[i,setI]=useState<any[]>([]),[q,setQ]=useState(''),[tier,setTier]=useState('ALL');useEffect(()=>{api.get('/institutions').then(r=>setI(r.data))},[]);const rows=i.filter(x=>(tier==='ALL'||String(x.tier)===tier)&&`${x.name} ${x.expertise}`.toLowerCase().includes(q.toLowerCase()));return <div className="space-y-5"><div><h1 className="text-3xl font-black">Higher education network</h1><p className="text-slate-500 mt-1">Institution expertise is sourced from the supplied tier datasets. Tier influences matching, not the final winner.</p></div><div className="card p-4 flex flex-col md:flex-row gap-3"><div className="relative flex-1"><Search size={17} className="absolute left-3 top-3 text-slate-400"/><input className="input pl-10" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search institution or expertise"/></div><select className="input md:w-44" value={tier} onChange={e=>setTier(e.target.value)}><option>ALL</option><option value="1">Tier 1</option><option value="2">Tier 2</option><option value="3">Tier 3</option><option value="4">Tier 4</option></select></div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{rows.map(x=><div className="card p-5" key={x.id}><div className="flex justify-between gap-3"><div className="h-10 w-10 rounded-xl bg-jharkhand-50 text-jharkhand-700 grid place-items-center"><GraduationCap size={19}/></div><Badge tone={x.tier===1?'blue':x.tier===2?'green':x.tier===3?'amber':'slate'}>Tier {x.tier}</Badge></div><h3 className="font-black mt-4">{x.name}</h3><div className="text-xs text-slate-400 mt-1">{x.location}</div><p className="text-sm text-slate-500 mt-3 line-clamp-3">{x.description}</p><div className="flex flex-wrap gap-1.5 mt-4">{x.expertise.slice(0,6).map((e:string)=><span className="text-[11px] px-2 py-1 rounded-full bg-slate-100 text-slate-600" key={e}>{e}</span>)}</div></div>)}</div></div>}
