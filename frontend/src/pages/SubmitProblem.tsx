import{Link}from'react-router-dom';import{ChevronLeft,ShieldCheck}from'lucide-react';import ProblemForm from'../components/forms/ProblemForm';export default function SubmitProblem(){return <div className="max-w-5xl mx-auto"><Link to="/dashboard" className="text-sm font-bold text-slate-500 inline-flex items-center gap-1"><ChevronLeft size={16}/>Back to dashboard</Link><div className="mt-5 mb-7"><h1 className="text-3xl md:text-4xl font-black">Report a civic problem</h1><p className="text-slate-500 mt-2">Share enough context for the platform to understand, validate and route the issue transparently.</p><div className="mt-4 flex gap-2 text-xs text-slate-500"><ShieldCheck size={15} className="text-jharkhand-600"/>AI analysis is advisory; human validation controls routing.</div></div><ProblemForm/></div>}
