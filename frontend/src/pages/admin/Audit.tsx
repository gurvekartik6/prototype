import{useEffect,useState}from'react';import{api}from'../../services/api';import{ScrollText}from'lucide-react';export default function Audit(){const[l,setL]=useState<any[]>([]);useEffect(()=>{api.get('/admin/audit-logs').then(r=>setL(r.data))},[]);return <div className="space-y-5"><div><h1 className="text-3xl font-black">Audit logs</h1><p className="text-slate-500 mt-1">Important platform actions are recorded for accountability.</p></div><div className="card divide-y">{l.length?l.map(x=><div className="p-5 flex gap-4" key={x.id}><div className="h-9 w-9 rounded-lg bg-slate-50 grid place-items-center"><ScrollText size={16}/></div><div><div className="font-bold">{x.action}</div><div className="text-xs text-slate-500 mt-1">{x.actorRole} • {x.entityType} • {x.entityId}</div><div className="text-xs text-slate-400 mt-1">{new Date(x.timestamp).toLocaleString()}</div></div></div>):<div className="p-10 text-center text-slate-500">No audit actions yet.</div>}</div></div>}
