import{useEffect,useState}from'react';import{api}from'../../services/api';import{BarChart,Bar,CartesianGrid,XAxis,YAxis,Tooltip,ResponsiveContainer,PieChart,Pie,Cell}from'recharts';import{Activity,ShieldCheck}from'lucide-react';export default function Analytics(){const[d,setD]=useState<any>();const[p,setP]=useState<any[]>([]);useEffect(()=>{Promise.all([api.get('/admin/dashboard'),api.get('/admin/problems')]).then(([a,b])=>{setD(a.data);setP(b.data)})},[]);const status=Object.entries(p.reduce((a:any,x:any)=>(a[x.status]=(a[x.status]||0)+1,a),{})).map(([name,value])=>({name,value}));const cat=Object.entries(p.reduce((a:any,x:any)=>(a[x.category]=(a[x.category]||0)+1,a),{})).map(([name,value])=>({name,value}));return <div className="space-y-5"><div><h1 className="text-3xl font-black">Platform analytics</h1><p className="text-slate-500 mt-1">Operational view for governance, routing and outcomes.</p></div><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">{[['Problems',d?.totalProblems],['Pending validation',d?.pendingValidation],['Active projects',d?.activeProjects],['Verified impact',d?.verifiedImpact]].map(([a,v])=><div className="card p-5" key={a}><div className="text-xs text-slate-400">{a}</div><div className="text-3xl font-black mt-2">{v||0}</div></div>)}</div><div className="grid lg:grid-cols-2 gap-5"><div className="card p-6"><div className="flex gap-3 items-center"><Activity className="text-jharkhand-600"/><h2 className="font-black">Problem status distribution</h2></div><div className="h-72 mt-5"><ResponsiveContainer width="100%" height="100%"><BarChart data={status}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name" tick={{fontSize:10}}/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="value" fill="#3aa76d" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="card p-6"><h2 className="font-black">Problem categories</h2><div className="h-72 mt-5"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={cat} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={90} label>{cat.map((_,i)=><Cell key={i} fill={['#3aa76d','#4f86c6','#8c72b8','#e6a23c','#6b7280'][i%5]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></div></div><div className="card p-6 flex items-center gap-3"><ShieldCheck className="text-jharkhand-600"/><div><div className="font-bold">Governance principle</div><div className="text-sm text-slate-500">AI output is advisory and distinguishable from human-approved decisions.</div></div></div></div>}
